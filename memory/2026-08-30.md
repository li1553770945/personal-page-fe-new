# 2026-08-30

## 今日结论

完成数据库博客的公开页面和超级管理员网页编辑器，并保留 Cloudflare Pages 作为唯一前端托管方式。文章发布后浏览器立即从后端读取，不依赖 Git 提交或前端重新构建；曾用于兼容动态 Next.js 的 Worker 已连同路由和项目删除。

## 前端实现

- 新增博客列表、文章详情、旧 permalink 兼容、导航和 sitemap；Markdown 支持 GFM 表格、数学公式、KaTeX、受控 raw HTML 和 VuePress 容器语法。
- 新增 `/admin/blog` 超级管理员入口，支持文章创建、编辑、草稿、发布/撤回、归档、删除、版本历史恢复、COS 图片上传和 AI 续写入口。
- 静态导出只生成稳定的 `/blog-shell` 客户端外壳；Pages `_redirects` 用 200 rewrite 将任意 `/blog/*` 请求映射到外壳，浏览器再按当前 URL 从后端读取文章，因此新 slug 无需重新部署。
- 保留 `/pages/*` 到 `/blog/:splat` 的 301 兼容和 `/usage/ai` 到管理页的永久跳转。

## 提交与部署

- 主功能提交：`f1ab34a`；纯 Pages 架构提交：`d13b0f9`，均已推送到 `origin/master`。
- `next.config.ts` 固定静态导出，已删除 OpenNext、Wrangler、Worker 配置和相关依赖。
- Cloudflare Pages 部署 `96d8b7d4` 成功解析 4 条 `_redirects` 规则并发布；自定义域 `peacesheep.xyz` 继续指向 Pages。
- Cloudflare Worker 路由 `peacesheep.xyz/*` 和 Worker 项目 `peacesheep-fe` 已删除；账户中的前端项目仅保留 Pages `personal-page-fe-new`。
- 旧 VuePress 仓库提交 `664da1a` 移除 Gitalk 明文 OAuth 配置并加入静态跳转；Cloudflare Zone 另建“旧博客文章迁移到新博客”和“旧博客其余页面迁移到新博客”两条原生 301 规则，不使用 Worker。

## 验证

- `corepack yarn build` 和 TypeScript 检查通过；静态构建生成 25 个路由。
- 生产 `/blog` 显示 96 个文章链接；`/blog/79465d` 生成 1 个 GFM 表格，`/blog/46dd8a` 生成 49 个 KaTeX 节点。
- `blog.peacesheep.xyz/pages/c36b98` 真实跳转到 `peacesheep.xyz/blog/c36b98` 并保留查询参数；旧博客首页跳转到新博客列表。
- `peacesheep.xyz/pages/c36b98` 也跳到对应新文章；未登录访问 `/admin/blog` 显示超级管理员权限拦截。
- API 边缘层已放行 `personal-page-fe-new.pages.dev` 和其单级预览子域，同时拒绝仿冒域。

## 已知事项

- 当前用户 Chrome 的扩展会把 `pages.dev` 页面到 `api.peacesheep.xyz` 的请求标记为 `ERR_BLOCKED_BY_CLIENT`；服务端 CORS 已通过 curl 精确验证，正式自定义域不受影响。若需要在该 Chrome 中直接使用预览域，应为此请求关闭对应拦截扩展或加入白名单。
- 旧仓库曾包含 GitHub OAuth client secret；虽然已从当前配置删除，但仍存在于历史提交中，应在 GitHub OAuth App 中撤销/轮换，不能只依赖删除最新文件。

